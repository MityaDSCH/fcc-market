'use strict';

import React from 'react';

var Chart = React.createClass({
  render: function() {
    return (
      <div>
        <svg id="chart">
        </svg>
      </div>
    );
  }
});

export default Chart;
